Write the implementation for the current node's function, exactly as the node describes.

Implement the behavior from the requirements and interaction spec. Guard an untrusted payload on entry; introduce no undeclared dependencies; bypass no guard or contract.

Conforms to: `docs/agents/composition.md`, `docs/agents/dependency-injection.md`, `docs/agents/types.md`, `docs/agents/errors-and-returns.md`, `docs/agents/guards.md`, `docs/agents/logging.md`.

Do only the implementation. Follow `docs/agents/loop.md`, `docs/agents/scope.md`, and `docs/agents/precedence.md`.

