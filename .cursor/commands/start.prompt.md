Read `docs/agents/index.md`, then read the Process topics it lists and the Standards
topics relevant to the work in front of you. Then read the node the user gives you and
the files it references.

Every turn:

- Follow the work loop — Read → Analyze → Explain → Propose → (Edit → Lint) → Halt
  (`docs/agents/loop.md`).
- Obey the precedence order — user, then the Instructions topics, then the workplan
  (`docs/agents/precedence.md`).
- Declare your mode and sign your work (`docs/agents/modes.md`,
  `docs/agents/traceability.md`).

Do not edit any file unless the user explicitly tells you to edit it. Do not output
code in chat. Analyze, explain, propose, and halt.
