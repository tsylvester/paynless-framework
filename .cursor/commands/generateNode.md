Generate a valid node and insert it under the end of the existing work ticket / description of requirements. It must detail, in the exact order given by the example checklist, every non-file section of a work node, and every relevant file-section of a work node. It must fully, explicitly detail the exact requirements for transforming the file to match the work ticket / description of requirements. Those detailed transform must exactly comply with the rules file for code standards. It must be grounded in actual requirements and transforms - no "check if" or "validate that". No hand-waving "change this to something else" directives. The node must be sufficiently complete that the implementer can read only the node and the file it references and know exactly what changes to make without having to perform additional research on the application or related files. 