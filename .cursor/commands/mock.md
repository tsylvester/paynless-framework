Update the mock file to move any factories, builders, or helpers out of tests and into the mock so they can be used by callers. 

Ensure that the mock is complete and provides the entire controllable function and type surface so that callers can correctly mock the function for unit tests. 