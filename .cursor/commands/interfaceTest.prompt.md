Write the interface test for the current node's function, exactly as the node describes.

Prove the contract by typed assignment — type membership, the return union's arms and
flavors, invariants. The interface not yet providing the symbols is the RED deliverable:
report the compiler errors and stop. Never create or edit the interface file. Every block
carries the collapsed one-line `Contract` header naming the membership or surface it
proves — no other fields, no inline markers, and no invented action.

Conforms to: `docs/agents/tests.md#interface`, `docs/agents/composition.md`,
`docs/agents/types.md`, `docs/agents/errors-and-returns.md`.

Do only the interface test. Follow `docs/agents/loop.md` and `docs/agents/precedence.md`.
