/**
 * Placeholder type for a navigation function, typically provided by a router library.
 * It accepts a path string and navigates the user.
 */
export type NavigateFunction = (path: string) => void; 